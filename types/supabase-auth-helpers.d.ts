declare module '@supabase/auth-helpers-nextjs' {
  // Minimal stub for environments where the package is not installed.
  // For full functionality, install the real package.
  export function createServerComponentClient(opts: any): any
}
